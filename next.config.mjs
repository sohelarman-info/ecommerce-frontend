/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "image.made-in-china.com",
      "i5.walmartimages.com",
      "www.techlandbd.com",
    ],
  },
};

export default nextConfig;
